# Clerk Authentication
# Get keys from https://dashboard.clerk.com/ (sg-recall application)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx

# 注意:
# - VITE_CLERK_PUBLISHABLE_KEY のみクライアント (.env.local / GH Actions secret) で必要
# - CLERK_SECRET_KEY はサーバー側のみ。本SPAでは使用しない
